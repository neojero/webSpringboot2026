# Active le profil "test"
spring.profiles.active=test

# Désactive les appels réels à l'API externe (on va les mocker)
custom.api.url=http://mock-api-url

# Port fixe pour les tests (optionnel, sinon utilise RANDOM_PORT)
server.port=9001

